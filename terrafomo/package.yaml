_include: !include '../share/package.yaml'

name: terrafomo

synopsis: Pending

description: >
  Pending

<<: *common
<<: *version
<<: *options
<<: *extensions

data-files:
  test/fixtures/*.tf

dependencies:
  - base >= 4.9

library:
  source-dirs:
    - src

  exposed-modules:
    - Paths_terrafomo
    - Terrafomo
    - Terrafomo.Core
    - Terrafomo.HCL
    - Terrafomo.HIL
    - Terrafomo.JSON
    - Terrafomo.Lens
    - Terrafomo.Monad

  dependencies:
    - aeson
    - hashable
    - microlens
    - mmorph
    - prettyprinter
    - random
    - scientific
    - text
    - transformers
    - unordered-containers

tests:
  hspec:
    main: Main.hs

    source-dirs: test

    dependencies:
      - aeson
      - hashable
      - hspec
      - hspec-discover
      - microlens
      - terrafomo
      - text
      - unordered-containers
