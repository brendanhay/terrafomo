_include: !include '../share/package.yaml'

name: terrafomo

synopsis: Pending

description: >
  Pending

<<: *common
<<: *version
<<: *options
<<: *extensions

data-files:
  test/fixtures/*.tf

dependencies:
  - base >= 4.9

library:
  source-dirs:
    - src

  exposed-modules:
    - Paths_terrafomo
    - Terrafomo
    - Terrafomo.DSL
    - Terrafomo.Encode
    - Terrafomo.HCL
    - Terrafomo.HIL
    - Terrafomo.Lens
    - Terrafomo.Schema
    - Terrafomo.Validate

  dependencies:
    - aeson
    - aeson-pretty
    - bytestring
    - hashable
    - microlens
    - mmorph
    - mtl
    - random
    - scientific
    - text
    - unordered-containers

    - optparse-applicative
    - process

    - free

tests:
  hspec:
    main: Main.hs

    source-dirs: test

    dependencies:
      - amazonka-iam-policy
      - bytestring
      - hashable
      - hspec
      - hspec-discover
      - microlens
      - terrafomo
      - unordered-containers
