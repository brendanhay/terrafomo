_include: !include '../share/package.yaml'

name: terrafomo

synopsis: Pending

description: >
  Pending

<<: *common
<<: *version
<<: *options
<<: *extensions

data-files:
  test/fixtures/*.tf

dependencies:
  - base >= 4.9

library:
  source-dirs:
    - src

  exposed-modules:
    - Terrafomo
    - Terrafomo.Builtin
    - Terrafomo.Core
    - Terrafomo.Encode
    - Terrafomo.Lens
    - Terrafomo.Monad
    - Terrafomo.Render
    - Terrafomo.Validate
    - Paths_terrafomo

  dependencies:
    - aeson
    - contravariant
    - hashable
    - microlens
    - mmorph
    - prettyprinter
    - random
    - text
    - transformers
    - unordered-containers

tests:
  hspec:
    main: Main.hs

    source-dirs: test

    dependencies:
      - hspec
      - hspec-discover
      - microlens
      - terrafomo
      - text
