_include: !include '../share/package.yaml'

name: terrafomo

synopsis: Pending

description: >
  Pending

<<: *common
<<: *version
<<: *options
<<: *extensions

data-files:
  test/fixtures/*.tf

dependencies:
  - base >= 4.9

library:
  source-dirs:
    - src

  exposed-modules:
    - Paths_terrafomo
    - Terrafomo
    - Terrafomo.HCL
    - Terrafomo.HIL
    - Terrafomo.Schema
    - Terrafomo.Encode
    - Terrafomo.Validate
    - Terrafomo.Monad
    - Terrafomo.Lens

  dependencies:
    - aeson
    - aeson-pretty
    - bytestring
    - hashable
    - microlens
    - mmorph
    - random
    - scientific
    - text
    - transformers
    - unordered-containers

tests:
  hspec:
    main: Main.hs

    source-dirs: test

    dependencies:
      - amazonka-iam-policy
      - bytestring
      - hashable
      - hspec
      - hspec-discover
      - microlens
      - terrafomo
      - unordered-containers
