name: AWS
package: terrafomo-aws

dependencies:
  - amazonka-core
  - formatting
  - iproute

# replace-datatype-fields:
#   provider:
#     region: P.Foo

# replace-matching-fields:
#   - suffix: cidr_block
#     type: TF.Attr s P.IPRange
