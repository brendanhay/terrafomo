package-name: terrafomo-aws
provider-name: AWS
partition-size: 80

extra-deps:
  - amazonka-core
  - formatting
  - iproute

# field-overrides:
#   - suffix: cidr_block
#     type: TF.Attr s P.

datatype-overrides:
  provider:
    region: P.Region
