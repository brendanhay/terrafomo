arguments:
  _name:
    required: true
    help: (Required) The name of the node being added to, or referenced in the catalog.
    ignored: false
    type: Text
  _address:
    required: true
    help: (Required) The address of the node being added to, or referenced in the
      catalog.
    ignored: false
    type: Text
name: consul_node
attributes:
  address:
    help: ! '- The address of the service.'
    type: Text
  name:
    help: ! '- The name of the service.'
    type: Text
about: Provides access to Node data in Consul. This can be used to define a node.
  Currently, defining health checks is not supported.
examples: []
