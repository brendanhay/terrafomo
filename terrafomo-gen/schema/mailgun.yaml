_name: MailgunProvider
arguments:
  _api_key:
    required: true
    help: (Required) Mailgun API key
    type: Text
    deprecated: false
attributes: {}
about: The Mailgun provider is used to interact with the resources supported by Mailgun.
  The provider needs to be configured with the proper credentials before it can be
  used. Use the navigation to the left to read about the available resources.
examples: []
