_name: digitalocean_domain
arguments:
  _ip_address:
    required: true
    help: (Required) The IP address of the domain. This IP is used to created an initial
      A record for the domain. It is required upstream by the DigitalOcean API.
    type: Text
  _name:
    required: true
    help: (Required) The name of the domain
    type: Text
attributes:
  id:
    help: ! '- The name of the domain'
    type: Text
about: Provides a DigitalOcean domain resource.
examples: []
