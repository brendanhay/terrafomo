arguments:
  _name:
    required: true
    help: (Required) The name of the Service object.
    ignored: false
    type: Text
  _hostname:
    required: true
    help: (Required) The host to check the service's status on
    ignored: false
    type: Text
  _check_command:
    required: true
    help: (Required) The name of an existing Icinga2 CheckCommand object that is used
      to determine if the service is available on the host.
    ignored: false
    type: Text
name: icinga2_service
attributes: {}
about: Configures an Icinga2 service resource. This allows service to be configured,
  updated, and deleted.
examples: []
