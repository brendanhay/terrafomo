arguments:
  _name:
    required: false
    help: (Optional) The name of the host. This can be a name or path. Can be omitted
      if there is only one host in your inventory.
    ignored: false
    type: Text
  _datacenter_id:
    required: true
    help: (Required) The </docs/providers/vsphere/index.html#use-of-managed-object-references-by-the-vsphere-provider>
      of a datacenter.
    ignored: false
    type: Text
name: vsphere_host
attributes: {}
about: The @vsphere_host@ data source can be used to discover the ID of a vSphere
  host. This can then be used with resources or data sources that require a host managed
  object reference ID.
examples: []
