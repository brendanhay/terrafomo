arguments:
  _name:
    required: true
    help: (Required) The name of the datacenter. This name needs to be unique within
      the folder. Forces a new resource if changed.
    ignored: false
    type: Text
  _folder:
    required: false
    help: (Optional) The folder where the datacenter should be created. Forces a new
      resource if changed.
    ignored: false
    type: Text
  _tags:
    required: false
    help: (Optional) The IDs of any tags to attach to this resource. See </docs/providers/vsphere/r/tag.html#using-tags-in-a-supported-resource>
      for a reference on how to apply tags.
    ignored: false
    type: Text
name: vsphere_datacenter
attributes: {}
about: Provides a VMware vSphere datacenter resource. This can be used as the primary
  container of inventory objects such as hosts and virtual machines.
examples: []
