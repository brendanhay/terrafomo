_name: rundeck_private_key
arguments:
  _key_material:
    required: true
    help: (Required) The private key material to store, serialized in any way that
      is accepted by OpenSSH.
    type: Text
  _path:
    required: true
    help: (Required) The path within the key store where the key will be stored.
    type: Text
attributes: {}
about: The private key resource allows SSH private keys to be stored into Rundeck's
  key store. The key store is where Rundeck keeps credentials that are needed to access
  the nodes on which it runs commands.
examples: []
