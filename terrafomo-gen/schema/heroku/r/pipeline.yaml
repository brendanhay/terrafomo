arguments:
  _name:
    required: true
    help: (Required) The name of the pipeline.
    ignored: false
    type: Text
name: heroku_pipeline
attributes:
  name:
    help: ! '- The name of the pipeline.'
    type: Text
  id:
    help: ! '- The UUID of the pipeline.'
    type: Text
about: Provides a <https://devcenter.heroku.com/articles/pipelines> resource. A pipeline
  is a group of Heroku apps that share the same codebase. Once a pipeline is created,
  and apps are added to different stages using <./pipeline_coupling.html> , you can
  promote app slugs to the next stage.
examples: []
