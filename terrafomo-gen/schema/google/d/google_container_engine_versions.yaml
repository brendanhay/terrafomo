_name: google_container_engine_versions
arguments:
  _project:
    required: true
    help: (optional) - ID of the project to list available cluster versions for. Should
      match the project the cluster will be deployed to. Defaults to the project that
      the provider is authenticated with.
    type: Text
    deprecated: false
  _zone:
    required: true
    help: (required) - Zone to list available cluster versions for. Should match the
      zone the cluster will be deployed in.
    type: Text
    deprecated: false
attributes:
  valid_master_versions:
    help: ! '- A list of versions available in the given zone for use with master
      instances.'
    type: Text
  latest_node_version:
    help: ! '- The latest version available in the given zone for use with node instances.'
    type: Text
  valid_node_versions:
    help: ! '- A list of versions available in the given zone for use with node instances.'
    type: Text
  latest_master_version:
    help: ! '- The latest version available in the given zone for use with master
      instances.'
    type: Text
about: Provides access to available Google Container Engine versions in a zone for
  a given project.
examples: []
