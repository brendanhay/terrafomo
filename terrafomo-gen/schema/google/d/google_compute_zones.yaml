arguments:
  _region:
    required: true
    help: (Optional) - Region from which to list available zones. Defaults to region
      declared in the provider.
    ignored: false
    type: Text
  _status:
    required: true
    help: (Optional) - Allows to filter list of zones based on their current status.
      Status can be either @UP@ or @DOWN@ . Defaults to no filtering (all available
      zones - both @UP@ and @DOWN@ ).
    ignored: false
    type: Text
name: google_compute_zones
attributes:
  names:
    help: ! '- A list of zones available in the given region'
    type: Text
about: Provides access to available Google Compute zones in a region for a given project.
  See more about <https://cloud.google.com/compute/docs/regions-zones/regions-zones>
  in the upstream docs.
examples: []
