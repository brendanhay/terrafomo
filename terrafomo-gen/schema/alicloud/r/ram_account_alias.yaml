arguments:
  _account_alias:
    required: true
    help: (Required, Forces new resource) Alias of cloud account. This name can have
      a string of 3 to 32 characters, must contain only alphanumeric characters or
      hyphens, such as "-", and must not begin with a hyphen.
    ignored: false
    type: Text
name: alicloud_ram_account_alias
attributes:
  account_alias:
    help: ! '- The account alias.'
    type: Text
about: Provides a RAM cloud account alias.
examples: []
