arguments:
  _instance_ids:
    required: true
    help: (Required, Forces new resource) The list of ECS instance's IDs.
    ignored: false
    type: Text
  _role_name:
    required: true
    help: (Required, Forces new resource) The name of role used to bind. This name
      can have a string of 1 to 64 characters, must contain only alphanumeric characters
      or hyphens, such as "-", "_", and must not begin with a hyphen.
    ignored: false
    type: Text
name: alicloud_ram_role_attachment
attributes:
  instance_ids:
    help: The list of ECS instance's IDs.
    type: Text
  role_name:
    help: ! '- The name of the role.'
    type: Text
about: Provides a RAM role attachment resource to bind role for several ECS instances.
examples: []
