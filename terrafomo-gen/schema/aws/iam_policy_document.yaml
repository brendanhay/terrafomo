_name: aws_iam_policy_document
arguments:
  _statement:
    required: true
    help: (Required) - A nested configuration block (described below) configuring
      one statement to be included in the policy document.
    type: Text
  _policy_id:
    required: true
    help: (Optional) - An ID for the policy document.
    type: Text
attributes: {}
about: Generates an IAM policy document in JSON format. This is a data source which
  can be used to construct a JSON representation of an IAM policy document, for use
  with resources which expect policy documents, such as the @aws_iam_policy@ resource.
examples: []
