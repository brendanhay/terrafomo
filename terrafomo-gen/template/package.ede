_include: !include '../../share/package.yaml'

name: {{ package }}

<<: *legal
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
{% for module in exposed %}
    - {{ module.value }}
{% endfor %}

  reexported-modules: >
    terrafomo:Terrafomo.Format,
    terrafomo:Terrafomo.Prelude
