_include: !include '../../share/package.yaml'

name: {{ package }}

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
{% for module in exposed %}
    - {{ module.value }}
{% endfor %}

  reexported-modules: >
    terrafomo:Terrafomo.Format
