_include: !include '../share/package.yaml'

name: terrafomo-gen

synopsis: Pending

description: >
  Pending

<<: *common
<<: *version
<<: *options
<<: *extensions

executables:
  terrafomo-gen:
    main: Main.hs

    source-dirs:
      - src

    dependencies:
      - aeson
      - base >= 4.9 && < 5
      - bytestring
      - containers
      - directory
      - ede
      - errors
      - filepath
      - mtl
      - optparse-applicative
      - pretty-show
      - split
      - terrafomo
      - text
      - unordered-containers
      - yaml
