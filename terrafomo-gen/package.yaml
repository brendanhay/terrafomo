_include: !include '../share/package.yaml'

name: terrafomo-gen

synopsis: Pending

description: >
  Pending

<<: *common
<<: *version
<<: *options
<<: *extensions

executables:
  terrafomo-gen:
    main: Main.hs

    source-dirs:
      - src

    dependencies:
      - aeson
      - aeson-pretty
      - base >= 4.9 && < 5
      - bytestring
      - directory
      - ede
      - errors
      - filepath
      - hashable
      - mtl
      - optparse-applicative
      - pretty-show
      - split
      - text
      - unordered-containers
      - word-wrap
      - yaml
