_include: !include '../../share/package.yaml'

name: terrafomo-rundeck

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Rundeck
    - Terrafomo.Rundeck.DataSource
    - Terrafomo.Rundeck.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
