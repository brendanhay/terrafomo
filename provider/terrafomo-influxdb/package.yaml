_include: !include '../../share/package.yaml'

name: terrafomo-influxdb

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.InfluxDB
    - Terrafomo.InfluxDB.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
