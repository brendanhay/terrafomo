_include: !include '../../share/package.yaml'

name: terrafomo-docker

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Docker
    - Terrafomo.Docker.DataSource
    - Terrafomo.Docker.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
