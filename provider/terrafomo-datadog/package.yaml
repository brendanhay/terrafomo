_include: !include '../../share/package.yaml'

name: terrafomo-datadog

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - microlens
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Datadog
    - Terrafomo.Datadog.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
