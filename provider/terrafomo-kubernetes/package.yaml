_include: !include '../../share/package.yaml'

name: terrafomo-kubernetes

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Kubernetes
    - Terrafomo.Kubernetes.DataSource
    - Terrafomo.Kubernetes.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
