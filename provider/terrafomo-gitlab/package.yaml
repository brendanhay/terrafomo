_include: !include '../../share/package.yaml'

name: terrafomo-gitlab

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Gitlab
    - Terrafomo.Gitlab.DataSource
    - Terrafomo.Gitlab.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
