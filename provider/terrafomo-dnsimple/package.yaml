_include: !include '../../share/package.yaml'

name: terrafomo-dnsimple

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.DNSimple
    - Terrafomo.DNSimple.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
