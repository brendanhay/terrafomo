_include: !include '../../share/package.yaml'

name: terrafomo-dnsmadeeasy

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - microlens
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.DNSMadeEasy
    - Terrafomo.DNSMadeEasy.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
