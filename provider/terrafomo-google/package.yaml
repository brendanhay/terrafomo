_include: !include '../../share/package.yaml'

name: terrafomo-google

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Google
    - Terrafomo.Google.DataSource
    - Terrafomo.Google.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
