_include: !include '../../share/package.yaml'

name: terrafomo-grafana

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Grafana
    - Terrafomo.Grafana.DataSource
    - Terrafomo.Grafana.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
