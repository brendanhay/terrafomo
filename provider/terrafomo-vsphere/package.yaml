_include: !include '../../share/package.yaml'

name: terrafomo-vsphere

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - microlens
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.VSphere
    - Terrafomo.VSphere.Data
    - Terrafomo.VSphere.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
