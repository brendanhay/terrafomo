_include: !include '../../share/package.yaml'

name: terrafomo-consul

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Consul
    - Terrafomo.Consul.DataSource
    - Terrafomo.Consul.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
