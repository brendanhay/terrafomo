_include: !include '../../share/package.yaml'

name: terrafomo-packet

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - microlens
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Packet
    - Terrafomo.Packet.Data
    - Terrafomo.Packet.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
