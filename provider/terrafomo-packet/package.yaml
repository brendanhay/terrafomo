_include: !include '../../share/package.yaml'

name: terrafomo-packet

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Packet
    - Terrafomo.Packet.DataSource
    - Terrafomo.Packet.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
