_include: !include '../../share/package.yaml'

name: terrafomo-statuscake

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - microlens
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.StatusCake
    - Terrafomo.StatusCake.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
