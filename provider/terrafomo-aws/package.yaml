_include: !include '../../share/package.yaml'

name: terrafomo-aws

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.AWS
    - Terrafomo.AWS.DataSource
    - Terrafomo.AWS.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
