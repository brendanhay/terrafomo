_include: !include '../../share/package.yaml'

name: terrafomo-aws

<<: *common
<<: *version
<<: *options
<<: *extensions

dependencies:
  - base
  - unordered-containers
  - text
  - terrafomo
  - formatting
  - amazonka-core
  - hashable
  - iproute
  - microlens

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.AWS

  reexported-modules: >
    terrafomo:Terrafomo,
    terrafomo:Terrafomo.Format
