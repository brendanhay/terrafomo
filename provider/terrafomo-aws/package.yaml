_include: !include '../../share/package.yaml'

name: terrafomo-aws

<<: *common
<<: *version
<<: *options
<<: *extensions

dependencies:
  - amazonka-core
  - base
  - formatting
  - hashable
  - iproute
  - microlens
  - terrafomo
  - text
  - unordered-containers

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.AWS

  reexported-modules: >
    terrafomo:Terrafomo,
    terrafomo:Terrafomo.Format
