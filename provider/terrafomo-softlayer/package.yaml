_include: !include '../../share/package.yaml'

name: terrafomo-softlayer

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.SoftLayer
    - Terrafomo.SoftLayer.DataSource
    - Terrafomo.SoftLayer.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
