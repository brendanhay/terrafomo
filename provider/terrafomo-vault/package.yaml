_include: !include '../../share/package.yaml'

name: terrafomo-vault

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Vault
    - Terrafomo.Vault.DataSource
    - Terrafomo.Vault.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
