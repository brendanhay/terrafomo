_include: !include '../../share/package.yaml'

name: terrafomo-nomad

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - microlens
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Nomad
    - Terrafomo.Nomad.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
