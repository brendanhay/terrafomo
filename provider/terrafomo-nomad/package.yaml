_include: !include '../../share/package.yaml'

name: terrafomo-nomad

<<: *common
<<: *version
<<: *options
<<: *extensions

dependencies:
  - base
  - hashable
  - microlens
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Nomad

  reexported-modules: >
    terrafomo:Terrafomo,
    terrafomo:Terrafomo.Format
