_include: !include '../../share/package.yaml'

name: terrafomo-rabbitmq

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - microlens
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.RabbitMQ
    - Terrafomo.RabbitMQ.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
