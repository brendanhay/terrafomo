_include: !include '../../share/package.yaml'

name: terrafomo-rabbitmq

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.RabbitMQ
    - Terrafomo.RabbitMQ.DataSource
    - Terrafomo.RabbitMQ.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
