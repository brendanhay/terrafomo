_include: !include '../../share/package.yaml'

name: terrafomo-arukas

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Arukas
    - Terrafomo.Arukas.DataSource
    - Terrafomo.Arukas.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
