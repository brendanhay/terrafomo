_include: !include '../../share/package.yaml'

name: terrafomo-powerdns

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.PowerDNS
    - Terrafomo.PowerDNS.DataSource
    - Terrafomo.PowerDNS.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
