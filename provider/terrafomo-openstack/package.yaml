_include: !include '../../share/package.yaml'

name: terrafomo-openstack

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.OpenStack
    - Terrafomo.OpenStack.DataSource
    - Terrafomo.OpenStack.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
