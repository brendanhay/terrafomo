_include: !include '../../share/package.yaml'

name: terrafomo-postgresql

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.PostgreSQL
    - Terrafomo.PostgreSQL.DataSource
    - Terrafomo.PostgreSQL.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
