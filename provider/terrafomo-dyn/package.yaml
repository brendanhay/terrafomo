_include: !include '../../share/package.yaml'

name: terrafomo-dyn

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Dyn
    - Terrafomo.Dyn.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
