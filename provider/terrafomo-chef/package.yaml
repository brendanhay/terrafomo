_include: !include '../../share/package.yaml'

name: terrafomo-chef

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Chef
    - Terrafomo.Chef.DataSource
    - Terrafomo.Chef.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
