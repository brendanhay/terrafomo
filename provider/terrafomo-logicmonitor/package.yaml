_include: !include '../../share/package.yaml'

name: terrafomo-logicmonitor

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - hashable
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.LogicMonitor
    - Terrafomo.LogicMonitor.DataSource
    - Terrafomo.LogicMonitor.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
