_include: !include '../../share/package.yaml'

name: terrafomo-opc

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.OPC
    - Terrafomo.OPC.DataSource
    - Terrafomo.OPC.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
