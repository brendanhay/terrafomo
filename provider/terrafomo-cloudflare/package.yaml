_include: !include '../../share/package.yaml'

name: terrafomo-cloudflare

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - containers
  - hashable
  - microlens
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Cloudflare
    - Terrafomo.Cloudflare.Data
    - Terrafomo.Cloudflare.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
