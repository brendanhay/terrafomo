_include: !include '../../share/package.yaml'

name: terrafomo-cloudflare

<<: *common
<<: *version
<<: *options

dependencies:
  - base
  - terrafomo
  - text

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terrafomo.Cloudflare
    - Terrafomo.Cloudflare.DataSource
    - Terrafomo.Cloudflare.Resource

  reexported-modules: >
    terrafomo:Terrafomo.Format
