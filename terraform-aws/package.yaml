_common: !include '../share/package-common.yaml'

name: terraform-aws

<<: *common

library:
  source-dirs:
    - src
    - gen

  exposed-modules:
    - Terraform.AWS
    - Terraform.AWS.Provider
    - Terraform.AWS.Types
    - Terraform.AWS.Resource

  reexported-modules: >
    terraform:Terraform.Format,
    terraform:Terraform.Prelude,
    terraform:Terraform.Tutorial
