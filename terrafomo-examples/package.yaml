_include: !include '../share/package.yaml'

name: terrafomo-examples

synopsis: Pending

description: >
  Pending

<<: *common
<<: *version
<<: *options

library:
  source-dirs:
    - src

  exposed-modules:
    - Terrafomo.Tutorial

  dependencies:
    - amazonka-core >= 1.4.5
    - base          >= 4.9 && < 5
    - terrafomo
    - terrafomo-aws
