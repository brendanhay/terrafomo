arguments:
- required: false
  help: (Optional) Specifies the IP address type (IPV4 or IPV6) and the IP address
    range (in CIDR format) that web requests originate from.
  name: ip_set_descriptors
  type: Text
- required: true
  help: (Required) The name or description of the IPSet.
  name: name
  type: Text
name: aws_waf_ipset
attributes:
- help: The ID of the WAF IPSet.
  name: id
  type: Text
