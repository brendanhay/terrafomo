arguments:
- required: true
  help: Specifies the set of compute environments mapped to a job queue and their
    order.  The position of the compute environments in the list will dictate the
    order. You can associate up to 3 compute environments with a job queue.
  name: compute_environments
  type: Attr Text
- required: true
  help: Specifies the name of the job queue.
  name: name
  type: Attr Text
- required: true
  help: The priority of the job queue. Job queues with a higher priority are evaluated
    first when associated with same compute environment.
  name: priority
  type: Attr Text
- required: true
  help: ! 'The state of the job queue. Must be one of:  ENABLED  or  DISABLED'
  name: state
  type: Attr Text
name: aws_batch_job_queue
attributes:
- help: The Amazon Resource Name of the job queue.
  name: arn
  type: Attr Text
