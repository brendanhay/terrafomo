arguments:
- required: true
  help: (Required) The ID of the Route Table to set as the new main route table for
    the target VPC
  name: route_table_id
  type: Text
- required: true
  help: (Required) The ID of the VPC whose main route table should be set
  name: vpc_id
  type: Text
name: aws_main_route_table_
attributes:
- help: The ID of the Route Table Association
  name: id
  type: Text
- help: Used internally, see   below
  name: original_route_table_id
  type: Text
