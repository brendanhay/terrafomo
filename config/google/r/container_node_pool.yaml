_name: google_container_node_pool
schema:
  arguments:
    cluster:
      required: true
      help: (Required) The cluster to create the node pool for.
      type: Text
    zone:
      required: true
      help: (Required) The zone in which the cluster resides.
      type: Text
  attributes: {}
