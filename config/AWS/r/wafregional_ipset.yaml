_name: aws_wafregional_ipset
schema:
  arguments:
    ip_set_descriptor:
      required: false
      help: (Optional) The IP address type and IP address range (in CIDR notation)
        from which web requests originate.
      type: Text
    name:
      required: true
      help: (Required) The name or description of the IPSet.
      type: Text
  attributes:
    id:
      help: The ID of the WAF IPSet.
      type: Text
