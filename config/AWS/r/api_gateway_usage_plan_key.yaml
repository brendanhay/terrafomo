_name: aws_api_gateway_usage_plan_key
schema:
  arguments:
    key_id:
      required: true
      help: (Required) The identifier of the API key resource.
      type: Text
    key_type:
      required: true
      help: (Required) The type of the API key resource. Currently, the valid key
        type is API_KEY.
      type: Text
    usage_plan_id:
      required: true
      help: (Required) The Id of the usage plan resource representing to associate
        the key to.
      type: Text
  attributes:
    key_id:
      help: The identifier of the API gateway key resource.
      type: Text
    key_type:
      help: The type of a usage plan key. Currently, the valid key type is API_KEY.
      type: Text
    usage_plan_id:
      help: The ID of the API resource
      type: Text
    value:
      help: The value of a usage plan key.
      type: Text
    name:
      help: The name of a usage plan key.
      type: Text
    id:
      help: The Id of a usage plan key.
      type: Text
