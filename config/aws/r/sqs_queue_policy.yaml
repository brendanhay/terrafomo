_name: aws_sqs_queue_policy
schema:
  arguments:
    queue_url:
      required: true
      help: (Required) The URL of the SQS Queue to which to attach the policy
      type: Text
    policy:
      required: true
      help: (Required) The JSON policy for the SQS queue
      type: Text
  attributes: {}
