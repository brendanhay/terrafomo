_name: aws_ecr_repository_policy
schema:
  arguments:
    repository:
      required: true
      help: (Required) Name of the repository to apply the policy.
      type: Text
    policy:
      required: true
      help: (Required) The policy document. This is a JSON formatted string.
      type: Text
  attributes:
    registry_id:
      help: The registry ID where the repository was created.
      type: Text
    repository:
      help: The name of the repository.
      type: Text
