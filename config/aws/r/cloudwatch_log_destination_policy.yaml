_name: aws_cloudwatch_log_destination_policy
schema:
  arguments:
    destination_name:
      required: true
      help: (Required) A name for the subscription filter
      type: Text
    access_policy:
      required: true
      help: (Required) The policy document. This is a JSON formatted string.
      type: Text
  attributes: {}
