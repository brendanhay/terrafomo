_name: aws_config_configuration_recorder_status
arguments:
  name:
    required: true
    help: (Required) The name of the recorder
    type: Text
  is_enabled:
    required: true
    help: (Required) Whether the configuration recorder should be enabled or disabled.
    type: Text
attributes: {}
about: ! 'Manages status (recording / stopped) of an AWS Config Configuration Recorder.
  ~> Note: Starting Configuration Recorder requires a </docs/providers/aws/r/config_delivery_channel.html>
  to be present. Use of @depends_on@ (as shown below) is recommended to avoid race
  conditions.'
examples: []
