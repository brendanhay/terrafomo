_name: aws_iam_account_alias
arguments:
  account_alias:
    required: true
    help: (Required) The account alias
    type: Text
attributes: {}
about: -> There is only a single account alias per AWS account.
examples:
- name: null
  code:
  - alias <- resource "alias" $
  - ! '    iam_account_alias_resource'
  - ! '        & account_alias .~ "my-account-alias"'
