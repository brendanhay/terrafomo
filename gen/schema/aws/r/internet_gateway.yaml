_name: aws_internet_gateway
arguments:
  vpc_id:
    required: true
    help: (Required) The VPC ID to create in.
    type: Text
  tags:
    required: false
    help: (Optional) A mapping of tags to assign to the resource.
    type: Text
attributes:
  id:
    help: ! '- The ID of the Internet Gateway.'
    type: Text
