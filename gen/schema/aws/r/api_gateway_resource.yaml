_name: aws_api_gateway_resource
arguments:
  path_part:
    required: true
    help: (Required) The last path segment of this API resource.
    type: Text
  rest_api_id:
    required: true
    help: (Required) The ID of the associated REST API
    type: Text
  parent_id:
    required: true
    help: (Required) The ID of the parent API resource
    type: Text
attributes:
  path:
    help: ! '- The complete path for this API resource, including all parent paths.'
    type: Text
  id:
    help: ! '- The resource''s identifier.'
    type: Text
about: Provides an API Gateway Resource.
examples:
- name: null
  code:
  - mydemoapi <- resource "mydemoapi" $
  - ! '    api_gateway_rest_api_resource'
  - ! '        & name .~ "MyDemoAPI"'
  - ! '        & description .~ "This is my API for demonstration purposes"'
  - ! ' '
  - mydemoresource <- resource "mydemoresource" $
  - ! '    api_gateway_resource_resource'
  - ! '        & rest_api_id .~ compute MyDemoAPI @"id"'
  - ! '        & parent_id .~ compute MyDemoAPI @"root_resource_id"'
  - ! '        & path_part .~ "mydemoresource"'
