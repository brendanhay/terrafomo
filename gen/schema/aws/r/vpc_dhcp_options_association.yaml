_name: aws_vpc_dhcp_options_association
arguments:
  vpc_id:
    required: true
    help: (Required) The ID of the VPC to which we would like to associate a DHCP
      Options Set.
    type: Text
  dhcp_options_id:
    required: true
    help: (Required) The ID of the DHCP Options Set to associate to the VPC.
    type: Text
attributes: {}
about: Provides a VPC DHCP Options Association resource.
examples: []
