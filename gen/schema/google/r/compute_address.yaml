_name: google_compute_address
arguments:
  name:
    required: true
    help: (Required) A unique name for the resource, required by GCE. Changing this
      forces a new resource to be created.
    type: Text
attributes:
  self_link:
    help: ! '- The URI of the created resource.'
    type: Text
  address:
    help: ! '- The IP of the created resource.'
    type: Text
about: Creates a static IP address resource for Google Compute Engine. For more information
  see <https://cloud.google.com/compute/docs/instances-and-network> and <https://cloud.google.com/compute/docs/reference/latest/addresses>
  .
examples:
- name: null
  code:
  - default <- resource "default" $
  - ! '    compute_address_resource'
  - ! '        & name .~ "test-address"'
