_name: kubernetes_pod
arguments:
  spec:
    required: true
    help: (Required) Spec of the pod owned by the cluster
    type: Text
  metadata:
    required: true
    help: ! '(Required) Standard pod''s metadata. More info: https://github.com/kubernetes/community/blob/master/contributors/devel/api-conventions.md#metadata'
    type: Text
attributes: {}
about: A pod is a group of one or more containers, the shared storage for those containers,
  and options about how to run the containers. Pods are always co-located and co-scheduled,
  and run in a shared context. Read more at https://kubernetes.io/docs/concepts/workloads/pods/pod/
examples: []
