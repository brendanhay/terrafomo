_name: kubernetes_config_map
arguments:
  data':
    required: false
    help: (Optional) A map of the configuration data.
    type: Text
  metadata:
    required: true
    help: ! '(Required) Standard config map''s metadata. More info: https://github.com/kubernetes/community/blob/master/contributors/devel/api-conventions.md#metadata'
    type: Text
attributes: {}
about: The resource provides mechanisms to inject containers with configuration data
  while keeping containers agnostic of Kubernetes. Config Map can be used to store
  fine-grained information like individual properties or coarse-grained information
  like entire config files or JSON blobs.
examples: []
