_name: vsphere_datacenter
arguments:
  folder:
    required: false
    help: (Optional) The folder where the datacenter should be created. Forces a new
      resource if changed.
    type: Text
  name:
    required: true
    help: (Required) The name of the datacenter. This name needs to be unique within
      the folder. Forces a new resource if changed.
    type: Text
  tags:
    required: false
    help: (Optional) The IDs of any tags to attach to this resource. See </docs/providers/vsphere/r/tag.html#using-tags-in-a-supported-resource>  for
      a reference on how to apply tags.
    type: Text
attributes: {}
