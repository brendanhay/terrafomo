_name: vault_aws_secret_backend
arguments:
  access_key:
    required: true
    help: (Required) The AWS Access Key ID this backend should use to issue new credentials.
    type: Text
  secret_key:
    required: true
    help: (Required) The AWS Secret Key this backend should use to issue new credentials.
    type: Text
attributes: {}
